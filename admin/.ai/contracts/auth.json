{
    "default_admin": {
        "email": "admin@example.com",
        "password": "password"
    },
    "endpoints": {
        "login": "/api/v1/login"
    },
    "notes": "Use /api/v1/login to obtain a Bearer token for endpoint testing. For backend testing, use the artisan command."
}
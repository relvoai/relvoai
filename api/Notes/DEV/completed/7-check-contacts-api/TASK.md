# Check Contacts API

## Description
The user asked for the location of the Contacts listing API. I initially failed to find `ContactController` or the `contacts.index` route. I need to locate where contacts are handled or implement the API if it's missing.

## Checklist
- [x] Search for `ContactController` (Found nothing)
- [x] Search for `contacts.index` route (Found nothing)
- [x] Implement `ContactController` (Created)
- [x] Define routes for Contacts (Registered)
- [x] Add tests (Created)
